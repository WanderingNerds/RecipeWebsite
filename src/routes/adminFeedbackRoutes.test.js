import test from "node:test";import assert from "node:assert/strict";process.env.SUPABASE_URL||="https://example.supabase.co";process.env.SUPABASE_ANON_KEY||="test";const {createAdminFeedbackRouter}=await import("./adminFeedbackRoutes.js");
test("all feedback handlers sit behind router-level admin middleware",()=>{const auth=()=>{};const r=createAdminFeedbackRouter({auth});assert.equal(r.stack[0].handle,auth);assert.equal(r.stack.filter(l=>l.route).length,3)});
test("invalid workflow input never creates a data client",async()=>{let made=false;const r=createAdminFeedbackRouter({auth(){},createClient(){made=true}});const layer=r.stack.find(l=>l.route?.path==="/:id"&&l.route.methods.post);const h=layer.route.stack[0].handle;const out={};await h({params:{id:"bad"},body:{status:"deleted"},flash(){}},{redirect(...v){out.redirect=v;return this}});assert.equal(made,false);assert.equal(out.redirect[0],303)});
